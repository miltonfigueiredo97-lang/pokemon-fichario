name: Apply Binder V6 Fix

on:
  push:
    paths:
      - 'tools/apply_v6.py'
      - '.github/workflows/apply-v6.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply V6 patch
        run: python tools/apply_v6.py
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo 'No changes'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html style.css script.js sw.js
          git commit -m 'Corrigir UX mobile, detalhes e scanner ao vivo'
          git push
